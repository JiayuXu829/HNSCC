# Reproducibility record

Record operating system, Python/environment lock, Git commit, configuration hashes, data-manifest hashes, random seeds, runtime, hardware, and exact commands for every reported run.
