# Phase 0 完成报告

**日期：** 2026-08-07  
**决策：** 通过（PASS）

## 已交付内容

- 建立了代码仓库结构，并设置了针对原始数据、中间数据、处理后数据和患者级预测结果的 Git 忽略规则。
- 建立了 Python 3.11 软件包骨架、固定版本的 `pyproject.toml` 和 `environment.yml`。
- 建立了各研究场景的配置文件以及分析冻结记录草案。
- 建立了数据 manifest、数据 schema 模板，以及 RADCURE、HANCOCK 和 TCGA/GEO 的完整空白审计包。
- 实现了仅依赖 Python 标准库的患者拆分、生存终点、哈希、文件清单和锁定测试治理代码。
- 建立了主文、补充材料、model card 和统计分析计划（SAP）占位文件，并明确禁止编造结果。

## 验证

运行命令：

```powershell
.\scripts\test.ps1
```

结果：使用 Python 3.11.8 运行，12 项测试全部通过。

其他验证：

- `python -m compileall -q src scripts tests`：通过。
- 尝试在分析未冻结时调用锁定测试：程序以 `analysis is not FROZEN` 拒绝执行，并返回非零退出状态。

## 锁定测试

未触碰锁定测试。尚未下载任何数据集，也未在本地查看任何结局数据表。

## 剩余风险

- 当前可用的系统 Python 3.11.8 随 Azure CLI 提供，不包含 `venv`；仍需在项目目录内安装完整的科研计算环境。
- 尚未获得下载固定版本工具和数据集的网络许可。
- ORCESTRA DOI/数据对象的解析，以及所有文件级许可证，仍需在实际下载后核验。

## 下一门槛

只有在获得项目本地固定版本下载的明确许可后，才能进入 Phase 1 数据获取。不得开始 Phase 2 建模。